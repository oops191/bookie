{
  "name": "bookie",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf-dev": "wrangler pages dev ./dist --d1=bookie-db --port 4321",
    "cf-preview": "wrangler pages dev ./dist --d1=bookie-db"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "@astrojs/rss": "^4.0.0",
    "@fontsource/inter": "^5.0.0",
    "astro": "^5.0.0",
    "jose": "^5.10.0",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
